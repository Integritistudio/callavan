/** Map header copy — edit these strings to change what users see on the map page. */

/** Shown when browsing as a customer (driver not logged in). */
export const CUSTOMER_MAP_HEADER = {
  title: 'See Who is Live Near You',
  subtitle: 'Local drivers. Real-time availability. Call directly',
};

/** Shown when a driver is logged in and live. */
export const DRIVER_MAP_HEADER_LIVE = {
  title: 'You are Now Live and Visible to the users',
  subtitle: 'You are sharing location and are live for users',
};

/** Shown when a driver is logged in but offline. */
export const DRIVER_MAP_HEADER_OFFLINE = {
  title: 'You are logged in',
  subtitle: 'Tap Go Live Now to become visible to users',
};

/** Shown when location permission is missing or denied. */
export const DRIVER_MAP_HEADER_NO_LOCATION = {
  title: 'You need to allow location to be visible on map',
  subtitle: 'You not visible to users',
};

/** Bottom banner when location has not been allowed. */
export const LOCATION_PERMISSION_BANNER = {
  message: 'Please allow your location to be visible on map',
};
